apiVersion: v1
kind: Secret
metadata:
  name: opencode-auth
  namespace: agentq
type: Opaque
stringData:
  auth.json: |
    {
      "providers": [
        {
          "provider": "openai",
          "api_key": "REPLACE_ME"
        }
      ]
    }
