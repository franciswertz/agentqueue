apiVersion: v1
kind: Service
metadata:
  name: playwright-mcp
  namespace: agentq
  labels:
    app: playwright-mcp
spec:
  type: ClusterIP
  selector:
    app: playwright-mcp
  ports:
    - name: http
      port: 3333
      targetPort: 3333
